<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WilliamApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:WilliamApp.Views"
    FlyoutBehavior="Flyout">

    <FlyoutItem Title="Inicio" Icon="home.png">
        <ShellContent ContentTemplate="{DataTemplate views:HomePage}" />
    </FlyoutItem>

    <FlyoutItem Title="Catálogo" Icon="catalogo.png">
        <ShellContent ContentTemplate="{DataTemplate views:CatalogoPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Carrito" Icon="carrito.png">
        <ShellContent ContentTemplate="{DataTemplate views:CarritoPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Mis Pedidos" Icon="pedidos.png">
        <ShellContent ContentTemplate="{DataTemplate views:MisPedidosPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Cuenta de Usuario" Icon="usuario.png">
        <ShellContent ContentTemplate="{DataTemplate views:CuentaUsuarioPage}" />
    </FlyoutItem>

</Shell>
